using System;

namespace UnitTestRecap
{
    public class AlbumTest
    {
        // Add a test for the RereleaseAlbum() method that follows these steps:
        // 1. Arrange - Create a new instance of the Album class with default
        //              values for all properties
        // 2. Act - Invoke the RereleaseAlbum() method on the instance
        // 3. Assert - Check with a constraint model assertion that the value of
        //             the ReleaseYear property equals the current year
        //             You can use DateTime.Now.Year to get the current year
        
        // Add a test for the IsA21stCenturyAlbum() method that follows these steps:
        // 1. Arrange - Create a new instance of the Album class where you specify
        //              the property values when calling the constructor. Use
        //              DateTime.Now.Year as a value for the ReleaseYear, all other
        //              values you can choose yourself.
        // 2. Act and Assert - Check with a constraint model assertion that the
        //                     result of the IsA21stCenturyAlbum() method, when
        //                     invoked on this instance, is equal to True.
    }
}
